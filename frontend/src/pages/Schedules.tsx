import type { Component } from 'solid-js';

const Schedules: Component = () => {
    return (
        <div class="w-screen h-screen flex items-center justify-center">
            <h1 class='text-3xl font-code'>These are the schedules!</h1>
        </div>
    );
};

export default Schedules;

