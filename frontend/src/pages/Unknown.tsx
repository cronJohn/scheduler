import type { Component } from 'solid-js';

const Unknown: Component = () => {
    return (
        <div class="w-screen h-screen flex items-center justify-center">
            <h1 class='text-3xl font-code'>404 RTFM</h1>
        </div>
    );
};

export default Unknown;
